## Templating Engines:
- **A template engine is a server-side tool that combines an HTML template with dynamic data to generate a final HTML page before sending it to the browser.**

- **In short:** **Template + Data → Template Engine → Final HTML → Browser.**

- There are some template engines as: EJS, Pug (Jade), Handlebars.